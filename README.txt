Replace these two files in your repo:
- components/KeyboardCard.js
- lib/keyboards.js

This removes broken local image paths and uses a built-in SVG fallback,
so your cards display immediately even if no real image files exist yet.
